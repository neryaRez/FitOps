{
  "name": "ProgressPhoto",
  "type": "object",
  "properties": {
    "userId": {
      "type": "string"
    },
    "date": {
      "type": "string",
      "format": "date"
    },
    "angle": {
      "type": "string",
      "enum": [
        "front",
        "side",
        "back"
      ]
    },
    "photoUrl": {
      "type": "string",
      "description": "URL from file upload service"
    }
  },
  "required": [
    "userId",
    "date",
    "angle",
    "photoUrl"
  ]
}